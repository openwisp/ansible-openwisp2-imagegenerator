#!/bin/bash
set -e
find {{ openwisp2fw_source_dir }}/bin/ \
     -iname '*-imagebuilder-{{ item.system }}-{{ item.subtarget }}*' \
     -exec cp {} {{ openwisp2fw_generator_dir }}/{{ item.system }}-{{ item.subtarget }}.gz \;
